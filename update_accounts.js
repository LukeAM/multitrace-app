const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); async function updateAccounts() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); const { data, error } = await supabase.from("accounts").update({ team_id: "team-user_2xgNSAQuuanSrhKwGfJHP6Dq5q4" }).filter("team_id", "is", null).select(); if (error) { console.error("Error updating accounts:", error); } else { console.log("Updated accounts:", data ? data.length : 0); } } updateAccounts();
